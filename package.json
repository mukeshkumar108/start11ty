{
  "name": "start11ty",
  "version": "1.0.0",
  "description": "starter eleventy baseline",
  "main": "index.js",
  "scripts": {
    "start": "npm run build-scss && npx @11ty/eleventy --serve",
    "build-scss": "sass src/scss:public/css --style=compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mukeshk",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "cssnano": "^6.0.3",
    "postcss": "^8.4.33",
    "sass": "^1.69.7"
  },
  "devDependencies": {
    "@11ty/eleventy-img": "^3.1.8",
    "bulma": "^0.9.4"
  }
}
